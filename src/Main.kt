
fun main() {
   val totalDistance = findLocationDistance(locationPoint1,locationPoint2)
    val similarityScore = findSimilarityScore(locationPoint1,locationPoint2)

println(totalDistance)
    println(similarityScore)

}